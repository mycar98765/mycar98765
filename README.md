    // Life motto
    final bool sad;
    final bool angry;
    if (sad == true) {
      stopSad();
      startHappy();
    } else if (angry == true) {
      stopAngry();
      startHappy();
    }
    // Let's be happy, not sad or angry
    // :)
